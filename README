NOTE: This project requires SoundHandler as a library.

Basically, this is just a way to test the functionality of SoundHandler on a device. It is very basic, just gives two buttons and a couple status messages. It streams a test sound file from my server.
